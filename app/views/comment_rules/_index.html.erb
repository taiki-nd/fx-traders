<% comments.each do |comment| %>
  <div class="comment_area" id="comment_<%= comment.id %>">
    <div>
      <%= comment.text %>
    </div>
    <div>
      <%= link_to '削除', rule_comment_rule_path(@rule, comment), method: :delete, remote: true, class: "link_to_user" %>
    </div>
  </div>
<% end %>