<section class="content">
  <h2 >みんなの取引記録</h2>
  <div class="grid">
    <% @records.each do |record| %>
      <%= render partial: "shared/records", locals: { record: record } %>
    <% end %>
  </div>
  <%= paginate @records %>
</section>

